# Changelog

## v0.1.3 — 2026-09-14

- 修复带 `IP-ASN` 规则的订阅预检超时：补齐经过官方 SHA-256 校验的 ASN 数据，统一在线安装、离线导入与候选目录。
- 旧安装在初始化续装及候选预检前自动补齐依赖，下载遵循 CLI 代理设置，不占用 60 秒内核预检预算。
- 预检错误保留超时/进程退出原因，识别大小写 ERROR/FATAL/panic；没有错误行时显示最近的脱敏初始化进度。
- 增加 ASN 下载与升级、候选链接、失败清理测试；Linux 夹具加入 IP-ASN，真实订阅测试扩展到节点 HTTPS 请求及原子热重载。

## v0.1.2 — 2026-09-13

- 修复大文件下载被 40 秒总超时截断：单次下载延长至 5 分钟，保留连接、TLS、响应头限时及失败重试。
- 下载支持 HTTP(S)/NO_PROXY 环境变量，普通用户自动 sudo 时显式保留；内核控制 API 始终直连。
- 下载不完整时显示已收字节数及脱敏的超时/连接错误，便于区分网络慢与内容损坏。
- 新增代理选择、sudo 参数、截断/超时重试回归及可选的非 root 在线组件安装验证。
- 文档增加慢网络初始化、手动 sudo 与定时任务代理配置说明。

## v0.1.1 — 2026-09-12

- 新增本仓库 `curl | sh` 安装入口，支持双架构、固定版本、SHA-256 校验与原子升级。
- 升级仅替换 CLI，保留运行内核与配置；安装失败自动清理并保留旧程序。
- 诊断检查实际内核健康与代理偏差，修复普通代理缺少 TUN 设备时的误报。
- 修复不相关环境变量导致代理状态误报；诊断可修复丢失的登录脚本。
- 日志脱敏覆盖 URL 路径/片段与 realm 分享链接，超长日志行不再挂起。
- 构建基线更新到 Go 1.26.8，更新 x/sys，CI/发布加入漏洞扫描和安装器验证。

## v0.1.0 — 2026-09-12

- Linux 交互式命令与 systemd 生命周期管理。
- GNOME/KDE 与持久登录环境系统代理，独立 TUN 开关。
- 多订阅、YAML/Base64/分享链接、策略组节点选择。
- 定时更新、依赖快照、原子替换重载、故障回滚与事务恢复。
- 状态、脱敏日志、诊断、可恢复卸载和 MetaCubeXD 面板。
- 双架构构建、真实 Linux 验收、模糊测试、组件校验与许可证归档。
